<%= simple_form_for :conversation, url: :conversations do |f| %>
  <%= f.input :recipients %>
  <%= f.input :subject %>
  <%= f.input :body, as: :text %>
  <div class="form-actions">
    <%= f.button :submit, 'Send', class: 'btn-default' %>
    <%= f.button :submit, 'Cancel', type: :reset, class: 'btn-danger' %>
  </div>
<% end %>
