== README
if postgres isn't working, shut down server and run:
pg_ctl -D /usr/local/var/postgres -l /usr/local/var/postgres/server.log start
then start server again :^)


This is a prototype social network built with Rails and hosted on Heroku. 

Go to alpha.extrara.com to see it in action!
